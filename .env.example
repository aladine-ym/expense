# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
CLIENT_ORIGIN=https://your-app-name.up.railway.app

# Session Security (CHANGE THIS IN PRODUCTION!)
SESSION_SECRET=your_super_secure_random_session_secret_here

# Database Configuration
SQLITE_PATH=./server/db/expensekeeper.sqlite

# Optional: Google OAuth (leave empty if not using)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://your-app-name.up.railway.app/auth/callback
